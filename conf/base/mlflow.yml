# MLflow configuration for Open Finance LakeHouse
# Minimal configuration to avoid Windows encoding issues

server:
  mlflow_tracking_uri: ./mlruns
  mlflow_registry_uri: ./mlruns

tracking:
  experiment:
    name: open-finance-lakehouse

ui:
  port: "5000"
  host: "127.0.0.1"
