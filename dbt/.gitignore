# dbt build artifacts
target/
dbt_packages/
logs/

# Per-machine anonymous install id dbt writes on first run.
.user.yml

# Local DuckDB databases: the writable dbt target and any local copy of the
# exported gold snapshot. Never commit data files.
*.duckdb
*.duckdb.wal
